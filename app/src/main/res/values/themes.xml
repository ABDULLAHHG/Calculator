<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.Calculator" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
    </style>

    <style name="Theme.Calculator" parent="Base.Theme.Calculator" />

    <style name="number_button" parent="Widget.AppCompat.Button">
        <item name="android:backgroundTint">@null</item>
        <item name="backgroundTint">@null</item>
        <item name="android:textColor">#554238</item>
        <item name="android:textSize">22sp</item>
        <item name="android:elevation">0dp</item>
        <item name="fontFamily">@font/doppioone_regular</item>
        <item name="android:layout_width">76dp</item>
        <item name="android:layout_height">76dp</item>
        <item name="android:stateListAnimator">@null</item>
        <item name="android:background">@drawable/bg_button</item>
    </style>

    <style name="operator_button">
        <item name="android:background">@drawable/bg_operation_button</item>
        <item name="android:backgroundTint">@null</item>
        <item name="backgroundTint">@null</item>
        <item name="android:textColor">#F8F8F8</item>
        <item name="android:textSize">22sp</item>
        <item name="fontFamily">@font/doppioone_regular</item>
        <item name="android:elevation">0dp</item>
        <item name="android:layout_width">76dp</item>
        <item name="android:layout_height">76dp</item>
        <item name="android:stateListAnimator">@null</item>
    </style>

</resources>